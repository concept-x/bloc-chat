//for modal (popup window) to create new chat room, per cp 3
//first, get modal window to pop up. Add a 'cancel' or 'close' button. Later, we'll pass the name in from the user.
//in Quote.js, where we add a new quote to array, firebase integration doesn't exist, so...
//...you'll need to add that functionality so the new quote stays part of the list when page refreshes.
//all of this is in Carrie's adascript repo -- bookmarked in Chrome.
